# 嘉祐資訊 CRM 管理系統簡報逐字稿

**簡報檔案：** `chiayou-crm/presentation.html`
**總頁數：** 14 頁
**建議時間：** 25~35 分鐘（視現場反應調整）

---

## 第 1 頁：封面

各位好，今天要為大家介紹的是「嘉祐資訊 CRM 管理系統」，這是我們從無到有、完整開發出來的一套客戶與維修管理系統。

CRM 是 Customer Relationship Management 的縮寫，簡單來說，就是用一套系統，把客戶資料、維修紀錄、庫存和出貨單全部管理起來。

今天我會從系統功能、架構設計、資料庫規劃、後端 API、前端開發、GitHub 管理、自動化部署，一路到最後的除錯經驗，完整說明一個系統從零到上線的整個過程。

---

## 第 2 頁：系統功能概覽

先來看這套系統能做什麼。

第一，**客戶管理**。98 筆客戶資料全部建檔，支援關鍵字搜尋，電話或姓名打幾個字就找得到，而且可以看到每一個客戶的維修歷史和出貨記錄，客戶來過幾次、修了什麼、花多少錢，全部一目了然。

第二，**維修管理**。每一張維修單都有完整的狀態追蹤，從「待處理」到「處理中」到「已完成」，什麼時候進廠、什麼時候結案，費用多少，全部有記錄。問題描述和維修過程是用所謂的「富文字編輯器」處理的，可以加粗體、斜體、項目符號，不是只能打普通文字。

第三，**庫存管理**。進貨成本、售價、數量、低庫存警示線，全部有記錄。什麼商品快賣完了，系統會提醒你。

第四，**出貨單管理**。選擇客戶、選擇商品項目、數量乘以單價自動計算總金額，最重要的是——可以列印出符合中一刀紙張的 PDF，這個功能我們會特別詳細說明。

最後，**儀表板 Dashboard**。老闆進系統第一眼就能看到：今天有幾筆待處理的維修、有哪些商品庫存快沒了、本月營收大概多少、最近出了哪些貨。不用一頁一頁翻，數據一次呈現。

---

## 第 3 頁：系統架構圖

接下來說技術層面。很多人聽到「做一個系統」就覺得很複雜，但其實架構可以很清晰。

這套系統分成**三層**。

**第一層：前端**，就是使用者看到的畫面。用的是 React 這個目前最主流的網頁框架，加上 TypeScript 來確保寫的程式不會有奇怪的錯誤，再用 Vite 來快速建構，用 Tailwind CSS 來快速刻畫面。部署在 Vercel，一個專門托管前端程式的平台，它的全球 CDN 會把畫面分發到各地，讓使用者打開網頁的速度很快。

**第二層：後端**，就是處理資料的地方。當使用者在畫面上點「新增客戶」，前端會發一個請求到後端，後端再去跟資料庫要資料或寫資料。用的是 Python 的 FastAPI 框架，效能很好，程式碼也清楚。部署在 Render，也是雲端主機。

**第三層：資料庫**，就是存放資料的地方。所有客戶、維修單、庫存資料，全部存在 Supabase 的 PostgreSQL 資料庫裡面。Supabase 是一家專門做「讓你不用自己管伺服器就能用資料庫」的公司，所以我們不需要自己架伺服器，資料庫的安全性、備份、災難復原，全部由他們處理。

**重要的一點**：前端和後端之間，後端和資料庫之間，全部是用 **HTTPS** 溝通，是加密的、安全的。攻擊者在中間攔截也看不懂內容。

---

## 第 4 頁：資料庫設計

再來看資料庫是怎麼設計的。

一個好的系統，資料庫設計是根基。如果資料庫亂寫，後面怎麼補救都很辛苦。

我們有**五張資料表**。

**customers 客戶表**。每一個客戶有一個 UUID 作為主鍵——UUID 是什麼？就像每個人的身分證字號，只是它是電腦自動產生的亂碼，格式像這樣：84636d00-465d-4476-8609-36aa7355afd7，好處是不會重複、也不會因為資料庫迁移而產生衝突。儲存姓名、電話（還有第二電話行動電話）、統一編號、地址、備註，備註欄位存的是 HTML 格式的富文字。

**repairs 維修單表**。每一筆維修單，會關聯到一個客戶——這叫做「外鍵」。維修單記錄：什麼裝置（筆電、桌機、螢幕）、什麼品牌、什麼型號、序號、問題描述、目前狀態（待處理/處理中/已完成/已取消）、維修過程、費用。

**inventory 庫存表**。每一個商品有商品編號、名稱、類別（CPU、硬碟、記憶體等）、數量、單位、成本、售價、供應商、低庫存警示線。進貨成本和售價是分開記的，這樣才能算出毛利。

**shipments 出貨單表**和 **shipment_items 出貨項目表**。一張出貨單可以包含很多個商品項目，這叫「一對多關係」。出貨單本身記錄誰買的、什麼日期、總金額；出貨項目記錄每一個商品的名稱（這裡存的是「快照」，因為商品名稱將來可能會改）、數量、單價、小計。

大家可以看到，這五張表彼此之間都有關聯：維修單關聯客戶、出貨單關聯客戶和商品。正是因為有這些關聯，我們才能做到「看到客戶就知道他所有歷史記錄」的功能。

---

## 第 5 頁：後端 API 設計

後端是整個系統的心臟。讓我說明後端是怎麼設計的。

FastAPI 這個框架，會把每一個「功能」定義成一個**路由 Route**。以客戶為例：
- GET /api/customers → 取得所有客戶（可加分頁、搜尋參數）
- GET /api/customers/{id} → 取得某一筆客戶
- POST /api/customers → 新增一筆客戶
- PUT /api/customers/{id} → 更新某一筆客戶
- DELETE /api/customers/{id} → 刪除某一筆客戶

這就是所謂的 **RESTful API 設計**，每一個 HTTP 方法（GET/POST/PUT/DELETE）對應一種操作，格式非常一致，國際標準，全世界工程師都看得懂。

**一個重要的技術細節**：一開始我們原本想直接用 SQLAlchemy 這個 ORM 工具，讓 Python 程式直接連接 PostgreSQL 資料庫。但是 Deploy 到 Render 以後，發現了一個大問題——**Render 這台雲端主機，根本無法直接連線到 Supabase 的 PostgreSQL port 5432**。因為 Supabase 的防火牆只允許特定 IP 連進去，而 Render 的 IP 是不固定的。

怎麼解決？我們放棄了直接連線的方式，改走 **HTTPS REST API**。Supabase 本身有提供一個叫做 PostgREST 的服務，它在 443 port 上監聽 HTTP 請求，你可以對它下 SQL 指令。例如你 POST 一筆資料過去，它就幫你寫進資料庫。

所以我們的 FastAPI 後端，其實並沒有直接操作資料庫，而是發 HTTP 請求給 Supabase 的 REST API，再由 Supabase 去操作 PostgreSQL。這樣做完全繞過了 port 5432 的限制，因為 443 port 是 HTTPS 標準 port，防火牆一定會開。

---

## 第 6 頁：前端架構

前端的部分，是大多數使用者接觸最多的地方。

前端用 React，搭配 **TypeScript**——TypeScript 的好處是，寫程式的時候，編譯器就會告訴你「這個地方型別錯誤」，而不是等到使用者操作到那一頁才爆炸。這叫做「編譯期檢查」，可以大幅降低上線後的 bug。

前端目錄結構很清晰。`pages/` 資料夾裡面放所有頁面，13 個檔案對應 13 個不同的頁面或功能。`services/api.ts` 把所有 API 呼叫封裝成一個個函數，前端任何地方需要抓客戶資料，就呼叫 `customerApi.getAll()`，不用每次都寫一長串 axios 的程式碼。

資料取得用的是 **React Query** 這個工具。它幫你處理「快取」、「背景更新」、「.loading 狀態」、「錯誤處理」，本來要寫幾十行才能做到的事情，用 React Query 三五行就解決了。

**環境變數**是前端一個很重要的概念。前端程式最終是散發到使用者的瀏覽器裡面執行的，所以「後端在哪裡」這個資訊必須在部署的時候就寫進去，不能寫死在程式碼裡面（否則從開發機器移到正式機器還要改程式，很危險）。我們把後端的 URL 存在 `VITE_API_URL` 這個環境變數裡面，部署的時候由 Vercel 平台幫我們注入。

---

## 第 7 頁：搜尋式下拉選單

接下來說一個很實用、使用者每天都會接觸的功能：**搜尋式下拉選單**。

在維修單和出貨單裡，你需要選擇「這筆維修/出貨是屬於哪個客戶」。一開始我們做的是傳統的下拉選單，就是點下去會跳出一個長長的列表那種。但問題來了——98 筆客戶，用下拉選單簡直是災難，要滾很久才能找到人。

後來改成搜尋式的，使用者只要在框裡打幾個字，例如「張」或「0912」，馬上就會過濾出符合的客戶。這個功能看起來簡單，但背後有幾個技術細節需要注意。

**第一**，怎麼知道使用者點了外面，而不是點了下拉裡面的項目？我們用一個叫做 `useRef` 的工具，綁定在下拉 DOM 元素上，然後監聽整個文件的 `mousedown` 事件。如果事件的 target 不在下拉元素裡面，就代表點了外面，自動把下拉關起來。

**第二**，編輯模式的時候，已經選好的客戶要正確顯示。例如這張維修單本來就是王大明的，編輯模式進去的時候，搜尋框裡要自動填上「王大明」，而不是空白。這就需要程式的 `useEffect` 鉤子，在元件掛載的時候，用已知的 customer_id 反過來去客戶陣列裡找名字，再填入搜尋框。

這個 pattern 已經推廣到維修單和出貨單的客戶選取，以及出貨單的商品選取，三個地方都在用。

---

## 第 8 頁：Rich Text 編輯器

另一個關鍵功能是**富文字編輯器**，就是在維修單和客戶資料裡，你可以用粗體、斜體、項目符號這種格式來寫備註，而不是只能打普通文字。

一開始我們用的是 `react-quill`，但上線之後發現了一個大問題——**React 19 出來之後，react-quill 無法使用**。原因是 react-quill 用了一個叫做 `findDOMNode` 的 API，這個 API 在 React 18 以後就被標記為 deprecated，在 React 19 直接被移除了。所以升級到 React 19 的網站，用 react-quill 會直接空白頁。

怎麼解決？我們換成了 **@tiptap/react**。這是一套專為 React 設計的模組化編輯器框架，你可以只選擇你需要的功能，不需要的功能不要加進來。例如我們只需要粗體、斜體、底線、項目符號、編號和連結，就只需要加一個 StarterKit 加上連結擴充就好。

另外一個細節：`@tiptap/extension-underline` 這個套件，必須保留在 package.json 裡面。為什麼？因為 StarterKit 雖然內建底線功能，但它的執行期需要用到 `underline` 這個擴充的程式碼。如果從 package.json 移除，build 會失敗。這個坑我們踩過一次，後來才發現原因。

---

## 第 9 頁：PDF 出貨單列印

這是整個系統裡面，**最多人問、也是我最花時間處理的功能**——出貨單的 PDF 列印。

為什麼特別拿出來講？因為要能夠在**點陣印表機**（例如 Epson LQ-300、LQ-670 這類老闆可能在用的印表機）上穩定輸出，是一件比想像中複雜的事情。

**第一個坑：瀏覽器列印引擎不可靠**。很多人直覺想到的是「我在網頁上放一個列印按鈕，按下去叫出瀏覽器的列印對話框」。這在某些環境下可以work，但問題是：不同的瀏覽器、不同的印表機驅動，對於 CSS `@page` 指令的解讀並不一致。尤其是在直印（Portrait）和紙張尺寸的設定上，Chrome 跟 Edge 常常給出不同的結果。而且點陣印表機通常沒有 PostScript 支援，對瀏覽器列印引擎的支援更差。

所以我們決定**不走瀏覽器的列印引擎，而是直接生成 PDF 檔案讓使用者下載**。這樣完全繞過了瀏覽器和印表機驅動的相容性問題。使用者下載 PDF 之後，用任何 PDF 閱讀器打開，都可以正確列印。

**第二個坑：jsPDF 的尺寸 bug**。jsPDF 是一個在瀏覽器裡生成 PDF 的 JavaScript 套件。我們需要生成一個 9.5 × 5.5 英吋的直印頁面（俗稱「中一刀」，是一種特殊的紙張規格）。我按照文件直覺寫 `format:[612, 396]`（612點 = 8.5英吋 × 72，396點 = 5.5英吋 × 72），再加上 `orientation:'portrait'`，結果輸出的 PDF 打開來是 5.5 × 8.5——寬高被交換了！這是 jsPDF 本身的 bug，文件裡根本沒寫。

後來trial and error 發現，**必須用 landscape 模式 + 90度旋轉** 才能得到正確結果。Landscape 模式下 jsPDF 的寬高不會交換，然後我把 html2canvas 截圖下來的畫布逆時針轉 90 度，再放進 PDF，這樣視覺上就變成了正確的 9.5 × 5.5 直印頁面。

這個過程，前後改了 10 個 commit，才得到一個完全正確的結果。

---

## 第 10 頁：GitHub 程式碼管理

接下來說**程式碼怎麼管**。

所有程式碼全部放在 **GitHub** 上面，一個叫 `yhe01368-hash/chiayou-crm` 的 repository。

為什麼要用 GitHub？因為它提供三個核心功能：**版本控制**、**多人協作**、**自動化部署**。

**版本控制**的意思是：每一次程式改動，都會留下一筆 record。例如今天改了客戶列表的搜尋功能，我可以下指令 `git commit -m "feat: 客戶列表新增電話搜尋"` 把這次改動「拍照」存起來。將來如果發現這個改動有問題，我可以一鍵回到上一個版本，所有原始碼全部恢復。

**多人協作**的意思是：將來如果系統變大，需要第二個工程師一起幫忙開發，GitHub 可以讓兩個人各自在自己的電腦上寫程式，最後再把改動合併在一起，不會覆蓋彼此的工作。

**自動化部署**的意思是：當我把程式 `git push` 上去，Vercel 和 Render 會自動收到通知，自動下載最新程式碼、執行建構、部署上線。這整個過程不需要我登入任何伺服器，不需要手動上傳檔案，push 之後兩三分鐘內，新功能就出現在正式網站上了。

目前 repository 裡有 12 個 commit，全部都有清楚的描述。例如「fix: left-align paper in screen view」「fix: expand paper width 8.5→9.5in 中一刀」「fix: jsPDF landscape+rotate 8.5×5.5」——只看 commit 訊息，就知道每次改動做了什麼。

---

## 第 11 頁：自動化部署流程

這頁詳細說明**自動化部署**是怎麼運作的。

整個流程是這樣的：

第一步，**開發者在本地端寫程式**。在自己的電腦上打 code，改功能或修 bug。

第二步，**git push 到 GitHub main 分支**。這一行指令，會把本地的所有改動上傳到 GitHub 伺服器。

第三步，**GitHub 通知 Vercel 或 Render**。GitHub 有一個叫做 Webhook 的功能，當收到新的程式碼時，會自動發 HTTP 請求通知我們設定好的 Vercel 和 Render。

第四步，**Vercel 自動部署前端**。Vercel 收到通知後，會啟動一個臨時的雲端機器，下載前端程式碼，安裝 npm 套件，執行 TypeScript 編譯，然後把產生出來的靜態檔案放到全球 CDN。

第五步，**Render 自動部署後端**。同樣收到通知，啟動雲端機器，安裝 Python 套件，啟動 FastAPI 服務。

全程不需要人工干預。工程師睡覺的時候，系統也可以自動更新。

**前端** Vercel 的環境變數只需要設定一個：`VITE_API_URL = https://chiayou-crm.onrender.com/api`，讓前端知道後端在哪裡。

**後端** Render 需要設定兩個：`SUPABASE_URL` 和 `SUPABASE_KEY`，讓後端能夠存取資料庫。

這三個平台——Vercel、Render、Supabase——加起來，每個月的費用是多少？**零元**。三個平台都有提供免費方案，對一個小型 CRM 系統來說，免費方案完全夠用。

---

## 第 12 頁：開發過程中的關鍵除錯經驗

這頁我想分享幾個**在開發過程中最具挑戰性的 bug**，以及我們是怎麼解決的。這些經驗也是整個簡報最有價值的部分——別人踩過的坑，你不用再踩一次。

**第一個：雲端主機無法直連資料庫**。這個問題前面有提到。Render 無法直接連線 Supabase PostgreSQL port 5432。我一開始的直覺反應是：密碼錯誤吧？金鑰設定錯了吧？不斷地檢查環境變數。折騰了好幾個小時，才意識到問題不是「設定錯誤」，而是**網路架構本身就阻擋了這條連線**。這時候必須改變策略，繞路走 HTTPS REST API，而不是繼續嘗試在死胡同裡找出路。

**第二個：Dashboard 同時爆炸三個 bug**。有段時間，Dashboard 的 API 完全無法運作，回傳 500 錯誤。我以為是一個問題，結果後來發現是三個獨立的問題同時存在：filter 的 `eq.` 前綴被加了两層變成 `eq.eq.`；`in()` 裡面的逗號被 URL 編碼成 `%2C`；Pydantic 的 `Decimal` 型別無法序列化。三個問題症狀都是同一個：HTTP 500。三個問題同時出現讓我一開始完全摸不著頭腦。後來我學會了：**一次只改一個變數**，先隔離問題，每個問題單獨處理，全部修完之後再一起測試。

**第三個：PDF jsPDF 的尺寸 bug**，前面已經說過了，前後 10 個 commit 才修好。關鍵在於，jsPDF 的文件不完整，很多 bug 文件根本沒寫，只能靠不斷地實驗和猜測，最後用逆向工程的方式確認了參數交換的規律。

**第四個：React 19 + react-quill 不相容**。這個問題比較特別，不是程式碼寫錯，而是**工具本身的版本跟最新技術不相容**。及時發現、及時替換成 @tiptap，避免了更大的災難。

**核心心法**：雲端環境和本地環境差異很大。很多「在我的電腦上明明正常」的程式，放到雲端就失敗，原因可能是網路限制、檔案系統不同、依賴版本不同。遇到問題的時候，不要假設任何事情是理所当然的，也不要假設文件一定正確，**一次只改一個變數，隔離問題，逐步逼近根本原因**——這是雲端除錯最重要的原則。

---

## 第 13 頁：專案成果與技術指標

經過這些努力，系統已經完全上線運行。

讓我報一些數字：

**4 個資料模組**：客戶、維修、庫存、出貨單。
**13 個前端頁面**：列表、表單、詳情、編輯、列印頁面，全部響應式設計，手機也可以用。
**5 個 API 端點群**：customers、repairs、inventory、shipments、dashboard，每個都是完整的 CRUD。
**98 筆已匯入客戶資料**：從 PDF 批次匯入，不是手key in的，是用 Python 程式自動化處理的。

**技術棧**：前端用 React 18、TypeScript、Vite 5、Tailwind CSS、React Router、React Query、@tiptap；後端用 Python、FastAPI、httpx、Pydantic；資料庫用 PostgreSQL + Supabase；部署用 Vercel + Render + GitHub。

**自動化程度**：push 到 GitHub 就自動部署前端和後端，不需要任何手動操作。Supabase 全托管，資料庫不需要自己管。PDF 生成在瀏覽器端完成，不佔用伺服器效能。

大家現在可以訪問 **chiayou-crm.vercel.app** 來看實際運作的系統。這就是整個系統的最終成果。

---

## 第 14 頁：總結

最後做一個總結。

今天介紹的這套系統，從無到有完整走過了：**需求訪談 → 系統設計 → 資料庫規劃 → API 建構 → 前端開發 → GitHub 版本控制 → 自動化部署 → 功能迭代 → 除錯優化**。

整個過程下來，我最深的體會是：**做系統不是一次到位的，而是持續迭代的**。沒有人能第一次就想到所有需求，也沒有系統上線後完全不需要改。重要的是：有一個好的架構、好的版本控制、好的部署流程，你就可以不斷地優化它，而不需要每次都從零開始。

而且很重要的一點：**全部使用免費工具**。Vercel、Render、Supabase，三個平台湊起來一個月不用付錢，對小型店家來說，是完全可以負擔的解决方案。

謝謝大家。如果有任何問題，歡迎提出來討論。

---

## 附錄：簡報操作說明

**開啟方式**：用瀏覽器打開 `chiayou-crm/presentation.html`

**操作方式**：
- **下一頁**：點右側箭頭按鈕，或鍵盤 → 或空白鍵
- **上一頁**：點左側箭頭按鈕，或鍵盤 ←
- **到第一頁**：Home 鍵
- **到最後一頁**：End 鍵
- **平板/手機**：左右滑動

**建議**：先在電腦上全螢幕開著，用鍵盤操作。讲的時候可以用雙螢幕，一個螢幕放簡報，一個螢幕放講稿參考。
